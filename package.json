{
  "name": "twinfold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Did your backup actually work? Private, offline folder comparison.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "start": "node scripts/serve.mjs",
    "format": "prettier --write src scripts tests"
  },
  "dependencies": {
    "@noble/hashes": "^2.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "prettier": "^3.9.7"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
